# Gateway — API reverse proxy, north-south entrypoint for /api (Ch 11-12).
apiVersion: apps/v1
kind: Deployment
metadata:
  name: gateway
  namespace: tickethub
  labels: { app: gateway }
spec:
  replicas: 3
  selector:
    matchLabels: { app: gateway }
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  template:
    metadata:
      labels: { app: gateway }
    spec:
      securityContext:
        runAsNonRoot: true
        seccompProfile: { type: RuntimeDefault }
      containers:
        - name: gateway
          image: registry.internal/tickethub/gateway:v1
          ports:
            - { name: http, containerPort: 8080 }
          envFrom:
            - configMapRef: { name: gateway-config }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
          resources:
            requests: { cpu: "100m", memory: 128Mi }
            limits:   { cpu: "500m", memory: 512Mi }
          readinessProbe:
            httpGet: { path: /readyz, port: 8080 }
            initialDelaySeconds: 3
            periodSeconds: 5
          livenessProbe:
            httpGet: { path: /healthz, port: 8080 }
            initialDelaySeconds: 10
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: gateway
  namespace: tickethub
spec:
  selector: { app: gateway }
  ports:
    - { name: http, port: 8080, targetPort: 8080 }
