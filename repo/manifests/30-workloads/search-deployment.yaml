# Search — event search API, queried by the catalog service (Ch 11-12).
apiVersion: apps/v1
kind: Deployment
metadata:
  name: search
  namespace: tickethub
  labels: { app: search }
spec:
  replicas: 2
  selector:
    matchLabels: { app: search }
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  template:
    metadata:
      labels: { app: search }
    spec:
      securityContext:
        runAsNonRoot: true
        seccompProfile: { type: RuntimeDefault }
      containers:
        - name: search
          image: registry.internal/tickethub/search:v1
          ports:
            - { name: http, containerPort: 8080 }
          envFrom:
            - configMapRef: { name: search-config }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
          resources:
            requests: { cpu: "100m", memory: 128Mi }
            limits:   { cpu: "500m", memory: 512Mi }
          readinessProbe:
            httpGet: { path: /readyz, port: 8080 }
            initialDelaySeconds: 3
            periodSeconds: 5
          livenessProbe:
            httpGet: { path: /healthz, port: 8080 }
            initialDelaySeconds: 10
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: search
  namespace: tickethub
spec:
  selector: { app: search }
  ports:
    - { name: http, port: 80, targetPort: 8080 }
